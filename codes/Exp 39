#include <stdio.h>

void try_all(char *cipher) {
    for (int k = 0; k < 26; k++) {
        for (int i = 0; cipher[i]; i++) {
            char p = ((cipher[i]-'A'-k+26)%26)+'A';
        }
    }
}
